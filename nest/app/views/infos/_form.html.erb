<%= form_with(model: info, local: true) do |form| %>
  <% if info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(info.errors.count, "error") %> prohibited this info from being saved:</h2>

      <ul>
      <% info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.number_field :address_id, id: :info_address_id %>
  </div>

  <div class="field">
    <%= form.label :street %>
    <%= form.text_field :street, id: :info_street %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
